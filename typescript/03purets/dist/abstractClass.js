"use strict";
class TakePhoto {
    constructor(cameraMode, filter) {
        this.cameraMode = cameraMode;
        this.filter = filter;
    }
}
const lokesh = new TakePhoto();
// class Instagram extends TakePhoto{
// }
